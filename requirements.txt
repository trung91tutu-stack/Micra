# ---- Bắt buộc ----
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
python-dotenv>=1.0

# ---- Giao diện web ----
streamlit>=1.30

# ---- Xuất tờ trình ra Word ----
python-docx>=1.1

# ---- Mô hình ngôn ngữ ----
google-genai>=1.0

# ---- KHÔNG cài trên máy chủ đám mây ----
# xgboost và shap nặng, dễ làm hết bộ nhớ 1 GB khi triển khai.
# MICRA tự chuyển sang GradientBoosting của scikit-learn, kết quả gần như không đổi.
# Muốn bật lại trên MÁY CÁ NHÂN thì bỏ dấu thăng ở hai dòng dưới:
# xgboost>=2.0
# shap>=0.44
